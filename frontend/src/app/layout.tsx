import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800"],
});

export const metadata: Metadata = {
  title: "DermaGenie — AI-Powered Skin Care Assistant",
  description:
    "Analyze your skin concerns, evaluate skincare ingredients, and receive intelligent recommendations powered by AI.",
  keywords: [
    "skincare",
    "AI",
    "skin analysis",
    "ingredients",
    "dermatology",
    "OCR",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
